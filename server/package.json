{
    "name": "escape-from-dungeonkov-server",
    "version": "1.0.0",
    "description": "Colyseus Server for Escape from Dungeonkov",
    "main": "index.ts",
    "scripts": {
        "start": "ts-node-dev --respawn --transpile-only index.ts",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@colyseus/monitor": "^0.15.0",
        "@colyseus/schema": "^2.0.0",
        "colyseus": "^0.15.0",
        "cors": "^2.8.5",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/cors": "^2.8.12",
        "@types/express": "^4.17.1",
        "@types/node": "^20.0.0",
        "ts-node": "^10.9.1",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.0.0"
    }
}